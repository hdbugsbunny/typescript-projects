export enum Methods {
  get = "get",
  post = "post",
  put = "put",
  del = "delete",
  patch = "patch",
}

export enum MetadataKeys {
  path = "path",
  method = "method",
  middleware = "middleware",
  validator = "validator",
}
